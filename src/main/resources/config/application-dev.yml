# TODO: mysql user and password (in application-dev and application-prod)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/j2etpapp?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: inserire_password
    driver-class-name: com.mysql.cj.jdbc.Driver